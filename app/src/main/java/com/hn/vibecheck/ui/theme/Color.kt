package com.hn.vibecheck.ui.theme

import androidx.compose.ui.graphics.Color

// Palet Gelap (Rekomendasi utama untuk aplikasi kamera)
val PixelDarkBackground = Color(0xFF1A1A24) // Biru dongker sangat gelap (Retro vibes)
val PixelDarkSurface = Color(0xFF252536) // Sedikit lebih terang untuk Card/Menu
val PixelCyan = Color(0xFF00E5FF) // Warna aksen utama (Primary) - Neon kalem
val PixelMagenta = Color(0xFFFF4081) // Warna aksen kedua (Secondary)

// Palet Terang (Jika user memaksa light mode)
val PixelLightBackground = Color(0xFFF4F5F8)
val PixelLightSurface = Color(0xFFFFFFFF)
val PixelPurple = Color(0xFF6200EA)
val PixelTeal = Color(0xFF03DAC5)

// Warna Fungsional
val PixelSuccess = Color(0xFF00E676) // Hijau terang untuk status "Pose Pas!"
val PixelError = Color(0xFFFF1744) // Merah untuk error/disconnect